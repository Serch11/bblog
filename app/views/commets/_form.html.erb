<%= form_with(model: commet, local: true) do |form| %>
  <% if commet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commet.errors.count, "error") %> prohibited this commet from being saved:</h2>

      <ul>
      <% commet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id, id: :commet_post_id %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :commet_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
